/**
 * BullMQ queue name constants.
 * Centralised so queue producers and processors always reference the same string.
 */

/** Name of the BullMQ queue that handles all outgoing email jobs. */
export const EMAIL_QUEUE = 'email';
